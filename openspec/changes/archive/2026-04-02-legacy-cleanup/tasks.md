## 1. 配置修复

- [x] 1.1 `.gitignore` 添加 `.DS_Store` 排除规则
- [x] 1.2 `common-dev/plugin.json` 移除 `"rules": "rules/"` capability

## 2. 文档更新

- [x] 2.1 `common-dev/README.md` 移除对已删除目录的引用（`.claude-plugin/`、`.codex-plugin/`、`sources/`、`mcp`、`rules/common.md`、`setup.js`），更新 capabilities 列表和安装说明，使文档与当前仓库结构一致
